# DRAFT — NOT APPROVED FOR PUBLICATION: résumé, LinkedIn, and Indeed copy

These variants are conditional. Do not publish or paste them into any profile until the repository/artifact is intentionally public or the wording explicitly and accurately says private, every numerical/browser claim is bound to retained evidence, and the owner approves the exact destination text.

## Résumé project entry

**SiteWipe — Browser Extension Safety Engineering** | JavaScript, HTML/CSS, Chrome MV3, Node.js

- Designed a fail-closed, site-scoped cleanup workflow using a bundled ICANN/PRIVATE Public Suffix List, read-only impact preflight, default detailed review plus an explicitly warned direct option, and short-lived single-use authorization across multiple browser APIs.
- Built interruption-safe temporary-permission and request-shield recovery, explicit partial/unknown verification states, privacy-preserving report retention/redaction, and adversarial unit/property/release-integrity tests.
- Created deterministic runtime/source packaging with explicit file closures, byte-parity verification, checksums, SBOM generation, version fingerprints, and evidence gates that prevent untested artifacts from inheriting browser claims.

Do not add test totals, coverage percentages, Chrome/Brave versions, performance figures, public/store links, or release language until those exact facts are retained and public.

## LinkedIn project description

SiteWipe is a browser-extension safety engineering project focused on a deceptively difficult question: how can destructive cleanup remain inside one freshly reviewed site boundary when cookies, storage, tabs, history, downloads, permissions, injected scripts, and network rules all have different scope and lifecycle semantics?

I designed the candidate around a locally bundled ICANN/PRIVATE Public Suffix List, a read-only impact preflight, default Standard/Expert detailed review, an explicitly warned opt-in direct mode that retains preflight binding, a context-bound single-use approval, per-adapter revalidation, interruption recovery, explicit verification uncertainty, and privacy-preserving local reports. The runtime is JavaScript/HTML/CSS with TypeScript-based JavaScript checking and a Node.js validation/release toolchain. Substantial AI-agent assistance is disclosed and treated as untrusted until checked by adversarial tests and review.

Publication, installed-browser results, and links must be added only after the exact artifact and repository are genuinely approved and public.

## Indeed résumé project description

Developed a public-source Manifest V3 browser-extension prerelease candidate for reviewed, target-scoped browser-data cleanup. Implemented Public Suffix List tenant isolation, single-use destructive authorization, browser-API scope guards, MV3 interruption recovery, report redaction/retention, explicit verification uncertainty, property tests, and deterministic release packaging. Production runtime uses JavaScript, HTML, and CSS; TypeScript is used for development-time checking.

## Short interview introduction

“I built SiteWipe to study safety boundaries in destructive browser tooling. The core work was not a cleanup button—it was preserving the same reviewed site scope across inconsistent browser APIs, worker suspension, temporary permissions, verification, privacy, and release evidence. I removed a convenient review-bypass design when the audit showed it weakened per-run consent, then added defense-in-depth tests so that kind of route fails closed.”

## Prohibited claims until evidence changes

- “published,” “released,” “open source,” “available on Chrome/Brave,” or any store/user count;
- “built with React” or “TypeScript extension”;
- “erases all traces,” “guaranteed clean,” “military-grade,” “anonymous,” or “zero data retained”;
- current browser compatibility, accessibility compliance, measured speed, vulnerability-free status, legal clearance, uniqueness, or owner-approved provenance without exact retained evidence;
- a test/coverage/artifact number copied from an older private candidate.
