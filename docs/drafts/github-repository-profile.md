# DRAFT — NOT APPROVED FOR PUBLICATION: GitHub repository presentation

## Proposed repository description

Safety-focused Manifest V3 browser extension for reviewed, site-scoped cleanup with explicit uncertainty and interruption recovery.

## Proposed topics

`browser-extension`, `chrome-extension`, `manifest-v3`, `privacy-engineering`, `security-engineering`, `javascript`, `public-suffix-list`, `property-testing`, `accessibility`

Do not add `typescript` as a primary implementation topic: production runtime is JavaScript/HTML/CSS; TypeScript is used only for development-time JavaScript checking. Do not add `brave` or store/release topics until exact-version installed evidence and publication exist.

## Proposed About-panel link

Leave blank until an owner-approved stable project/privacy URL exists. Do not point to a private machine path or an unapproved temporary host.

## Profile/pin rule

Do not add SiteWipe to a GitHub profile README or pin it until the repository is intentionally public, the exact claims are independently inspectable, all applicable publication gates pass, and the owner separately approves the profile change.

## README editorial position

Lead with the destructive-scope engineering problem and factual safeguards, not generic “privacy cleaner” marketing. Keep the private-candidate banner until actual publication. Preserve these limits near the top:

- no promise of complete erasure;
- browser/website/network/OS data can remain or return;
- runtime is JavaScript/HTML/CSS, not a TypeScript implementation;
- installed Chrome/Brave, accessibility, and performance claims require exact-artifact evidence;
- SiteWipe has exact-name marketplace collisions and is not represented as unique or legally cleared;
- substantial AI-agent assistance is disclosed.
